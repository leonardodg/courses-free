/**
 * Barra de secoes da landing de parceiros.
 *
 * Faz duas coisas, e as duas por medicao e nao por valor fixo:
 *
 * 1. Publica a altura do cabecalho FIXO do tema em --ldgp-sticky-top. A altura
 *    muda por tema, e no theme_ldg muda tambem entre celular e desktop.
 *    Hardcodar 64px quebra no Moove e quebra no celular do ldg.
 *
 * 2. Marca a secao em vista com aria-current. Por IntersectionObserver, e nao
 *    por evento de rolagem: o observer nao roda a cada pixel.
 *
 * O sticky em si e CSS. Este modulo so alimenta o numero de que o CSS precisa -
 * se ele nao carregar, a barra continua grudando, so que no topo do viewport.
 *
 * NAO ha transpilador neste projeto: este arquivo precisa ser AMD de verdade.
 *
 * @module     local_partners/sectionbar
 * @copyright  2026 LeoDG <callme@leodg.dev>
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_partners/sectionbar",[],(function(){var medirCabecalho=function(){var barra=document.querySelector(".ldgp-bar");if(barra){var cabecalho=document.querySelector(".navbar.fixed-top"),altura=cabecalho?Math.round(cabecalho.getBoundingClientRect().height):0;barra.style.setProperty("--ldgp-sticky-top",altura+"px");var pagina=document.querySelector(".ldgp");pagina&&pagina.style.setProperty("--ldgp-sticky-top",altura+"px")}},observarSecoes=function(){var links=Array.prototype.slice.call(document.querySelectorAll('.ldgp-bar-link[href^="#"]'));if(links.length&&window.IntersectionObserver){var porId={},secoes=[];links.forEach((function(link){var id=link.getAttribute("href").slice(1),secao=document.getElementById(id);secao&&(porId[id]=link,secoes.push(secao))}));var observer=new window.IntersectionObserver((function(entradas){entradas.forEach((function(entrada){var id;entrada.isIntersecting&&(id=entrada.target.id,links.forEach((function(link){link.removeAttribute("aria-current")})),porId[id]&&porId[id].setAttribute("aria-current","true"))}))}),{rootMargin:"-20% 0px -70% 0px",threshold:0});secoes.forEach((function(secao){observer.observe(secao)}))}};return{init:function(){var fn,espera,timer;medirCabecalho(),observarSecoes(),window.addEventListener("resize",(fn=medirCabecalho,espera=150,timer=null,function(){window.clearTimeout(timer),timer=window.setTimeout(fn,espera)}))}}}));

//# sourceMappingURL=sectionbar.min.js.map