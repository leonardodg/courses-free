<VirtualHost *:80>
    ServerAdmin callme@leodg.dev

    ServerName localhost
    ServerAlias ${MOODLE_URL_CLEAN}

    DocumentRoot ${APACHE_DOCUMENT_ROOT}

	# O /sitemap.xml e gerado pelo local_partners. Alias, e nao rewrite: nao
	# depende do mod_rewrite estar carregado, e o handler do PHP roda igual.
	Alias /sitemap.xml ${APACHE_DOCUMENT_ROOT}/local/partners/sitemap.php

    <Directory "${APACHE_DOCUMENT_ROOT}">
		Options FollowSymLinks
		AllowOverride All
		Require all granted
	</Directory>

	<DirectoryMatch "/(vendor|node_modules|tests/behat|\.git|\.devcontainer|\.github)(/|$)">
		Require all denied
	</DirectoryMatch>

	<FilesMatch "^(composer\.(json|lock)|package(-lock)?\.json|phpunit\.xml(\.dist)?|environment\.xml|install\.xml|README(\.txt|\.md)?|UPGRADING\.md|CHANGES\.md|\.env.*)$">
		Require all denied
	</FilesMatch>

    ErrorLog ${APACHE_LOG_DIR}/moodle_error.log
    CustomLog ${APACHE_LOG_DIR}/moodle_access.log combined
</VirtualHost>
